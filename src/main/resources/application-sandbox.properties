# Perfil sandbox — paralelo a dev. IntelliJ / jar: --spring.profiles.active=sandbox
server.port=8188

spring.datasource.url=jdbc:postgresql://localhost:5432/controlneg_rmx_db_sandbox
spring.datasource.username=romax-admin
spring.datasource.password=f4ast3rv3rs10n*

# Auth sandbox (no el :8081 de desarrollo)
auth.service.base-url=http://localhost:8181
auth.service.host=http://localhost:8181

# Mail compartido con dev
mail.service.host=http://localhost:8082

# Catálogo Mongo compartido (fotos/productos). Aislar después si hace falta.
# spring.data.mongodb.uri=mongodb://admin:admin@localhost:27017/test?retryWrites=true&loadBalanced=false&connectTimeoutMS=10000&authSource=admin&authMechanism=SCRAM-SHA-1

cors.allowed-origins=*
